import { FieldErrorMessagesPipe } from './field-error-messages.pipe';

describe('FieldErrorMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldErrorMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
